@model Inmobiliaria_.Net_Core.Models.Pago

@{ ViewData["Title"] = "Registrar Pago"; }

<h1>Registrar Pago</h1>

<hr />

<div class="row">
    <div class="col-md-6">
        <form asp-action="Crear" method="post">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>

            <div class="form-group mb-3">
                <label asp-for="Concepto" class="control-label"></label>
                <input asp-for="Concepto" class="form-control">
                <span asp-validation-for="Concepto" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="Importe" class="control-label"></label>
                <input asp-for="Importe" type="number" step="0.01" min="0" class="form-control">
                <span asp-validation-for="Importe" class="text-danger"></span>
            </div>

            <div class="form-group mb-3">
                <label asp-for="ID_Reserva" class="control-label">Reserva</label>
                <select asp-for="ID_Reserva" asp-items="ViewBag.Reservas" class="form-control">
                    <option value="">Seleccione una reserva</option>
                </select>
                <span asp-validation-for="ID_Reserva" class="text-danger"></span>
            </div>

            <div class="form-group">
                <input type="submit" value="Registrar" class="btn btn-success">
                <a asp-action="Indice" class="btn btn-secondary">Volver</a>
            </div>
        </form>
    </div>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}